" #############################################################################
" Fold settings
" #############################################################################

" fold column to show where folds are
set foldcolumn=0
" Auto fold when cursor leave
set foldclose=all
